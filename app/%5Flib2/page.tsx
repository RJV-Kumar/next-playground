/**
 * http://localhost:3001/%5Flib2
 * if we want underscore in our route then use %5F instead of _
 * now _lib2 works in browser
**/

export default function Lib() {
  return (
    <div> <h1>Lib Page</h1> 
    </div>
  );
}
