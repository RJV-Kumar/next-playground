/**
 * http://localhost:3001/login
 * (auth) will create route-group
 * now nextjs will consider as route-group and will not include in URL
**/

export default function Login() {
  return (
    <div> <h1>Login Page</h1> 
    </div>
  );
}